{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "allowJs": false,
  },
  "include": [
    "src/**/*.ts",
    "__tests__/**/*.ts",
    "./**/*.js"
  ]
}
